<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <!--
   |
   | This is an example pom for sitebricks sync model. Does not contain the client libraries.
   | Make sure you rename this pom.xml and then run mvn package to grab all the depedencies.
   |
   | You may want to add logback for viewing Jetty logs too.
   |-->
  <modelVersion>4.0.0</modelVersion>

  <!-- Rename these to suit your fancy! -->
  <groupId>org.sitebricks</groupId>
  <artifactId>example</artifactId>
  <name>My Sitebricks Project</name>

  <dependencies>
    <dependency>
      <groupId>com.google.sitebricks</groupId>
      <artifactId>sitebricks</artifactId>
      <version>0.8.5</version>
    </dependency>

    <!--
     Uncomment the following to add Sitebricks Web Client to your project, a simple alternative
     to Apache HTTP Client.
    -->
    <!--<dependency>-->
      <!--<groupId>com.google.sitebricks</groupId>-->
      <!--<artifactId>sitebricks-client</artifactId>-->
      <!--<version>0.8.5</version>-->
    <!--</dependency>-->

    <dependency>
      <groupId>org.mortbay.jetty</groupId>
      <artifactId>jetty</artifactId>
      <version>6.1.9</version>
    </dependency>
    <dependency>
      <groupId>org.mortbay.jetty</groupId>
      <artifactId>jetty-util</artifactId>
      <version>6.1.9</version>
    </dependency>
    <dependency>
      <groupId>org.mortbay.jetty</groupId>
      <artifactId>servlet-api-2.5</artifactId>
      <version>6.1.9</version>
    </dependency>
  </dependencies>
</project>
